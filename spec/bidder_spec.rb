require "spec_helper"

RSpec.describe Bidder do
  it "has a version number" do
    expect(Bidder::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
